/**
 * Public API of the shared UI segment (ADR 0065/0066) — reusable presentational pieces
 * that sit below the FSD slice layers and may be imported by any of them.
 */
export { ComboField } from "./ComboField";
