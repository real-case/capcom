/**
 * Public API of the `overview-dashboard` widget (ADR 0065/0066/0099) — the curated
 * mission-control Overview home. Consumers (the project overview route) import
 * `@/widgets/overview-dashboard`, never a deep segment path.
 */
export { OverviewDashboard } from "./ui/OverviewDashboard";
